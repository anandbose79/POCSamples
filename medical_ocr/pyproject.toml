[build-system]
requires = ["setuptools>=65"]
build-backend = "setuptools.build_meta"

[project]
name = "medical-ocr"
version = "0.1.0"
description = "Pipeline for extracting medical data from password-protected PDFs using OpenAI OCR"
readme = "README.md"
authors = [{name = "Generated by ChatGPT"}]
requires-python = ">=3.11"
dependencies = [
  "openai>=1.13.3",
  "pdf2image>=1.17.0",
  "pillow>=10.2.0",
]

[project.scripts]
medical-ocr = "medical_ocr.cli:main"
